import '../css/Chatpage.scss'
import React, { useState } from 'react';

const Send_messages = ([socket, username, room, language]) =>{
    const [message,setMessage] = useState('');

    const sendMessage()
}